apiVersion: v1
kind: Service
metadata:
  name: {{ include "awsxray-exporter.fullname" . }}
  labels:
    {{- include "awsxray-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "awsxray-exporter.selectorLabels" . | nindent 4 }}
